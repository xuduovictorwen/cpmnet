% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.cv.cpmnet}
\alias{plot.cv.cpmnet}
\title{Plot cross-validation curves from cv.cpmnet}
\usage{
\method{plot}{cv.cpmnet}(x, metric = NULL, sign.lambda = 1, ...)
}
\arguments{
\item{x}{fitted \code{"cv.cpmnet"} object.}

\item{metric}{which metric to plot. Only used when \code{x$type} is
\code{"mean"} or \code{"median"}; one of \code{"mae"} (default),
\code{"mse"}, or \code{"pR2"}. Ignored for other types.}

\item{sign.lambda}{\code{1} to plot against \code{log(lambda)} (the
default), \code{-1} to flip the x-axis.}

\item{...}{passed to \code{\link[graphics]{plot}}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Plots the selected CV metric (mean across folds) against log-lambda,
with vertical bars showing plus/minus one standard error. Dashed
lines mark \code{lambda.min} (black) and \code{lambda.1se} (blue).
}
\examples{
\donttest{
set.seed(1)
x <- matrix(rnorm(120 * 5), 120, 5)
y <- x[, 1] - x[, 2] + rnorm(120)
cvfit <- cv.cpmnet(x, y, family = "probit", nfolds = 5, type = "brier",
                   nlambda = 15, parallel = FALSE)
plot(cvfit)
}

}
\seealso{
\code{\link{cv.cpmnet}}, \code{\link{plot.cpmnet}}
}
