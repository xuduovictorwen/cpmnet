% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.cpmnet}
\alias{plot.cpmnet}
\title{Plot coefficient paths from a cpmnet fit}
\usage{
\method{plot}{cpmnet}(x, xvar = c("lambda", "norm", "dev"), label = FALSE, ...)
}
\arguments{
\item{x}{fitted \code{"cpmnet"} object.}

\item{xvar}{x-axis variable. One of \code{"lambda"} (log-lambda),
\code{"norm"} (L1 norm of the slope vector), or \code{"dev"}
(fraction deviance explained).}

\item{label}{if \code{TRUE}, label nonzero coefficients at the
rightmost point with their predictor index.}

\item{...}{passed to \code{\link[graphics]{plot}}.}
}
\value{
\code{NULL}, invisibly. Called for its side effect.
}
\description{
Produces a glmnet-style coefficient path plot. Each line traces one
predictor's slope across the penalization (regularization) path. A top-axis shows
the number of nonzero coefficients.
}
\examples{
\donttest{
set.seed(1)
x <- matrix(rnorm(100 * 5), 100, 5)
y <- x[, 1] + rnorm(100)
fit <- cpmnet(x, y, family = "probit", nlambda = 20)
plot(fit, xvar = "lambda")
}

}
\seealso{
\code{\link{cpmnet}}, \code{\link{plot.cv.cpmnet}}
}
